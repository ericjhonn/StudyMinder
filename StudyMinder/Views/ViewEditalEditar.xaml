<?xml version="1.0" encoding="UTF-8"?>
<UserControl x:Class="StudyMinder.Views.ViewEditalEditar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:views="clr-namespace:StudyMinder.Views"
             xmlns:models="clr-namespace:StudyMinder.Models"
             xmlns:converters="clr-namespace:StudyMinder.Converters"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=viewmodels:EditarEditalViewModel}">

    <Grid Background="{DynamicResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Cabeçalho -->
        <Border Grid.Row="0"
                Style="{DynamicResource ViewHeaderStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{Binding Title}"
                           Style="{DynamicResource Title1Style}" />
                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button Style="{StaticResource SecondaryButtonStyle}"
                            Command="{Binding CancelarCommand}"
                            VerticalAlignment="Center"
                            Margin="0,0,12,0"
                            IsEnabled="{Binding IsSaving, Converter={StaticResource InverseBooleanConverter}}">
                        <TextBlock Text="Cancelar" />
                    </Button>
                    <Button Style="{StaticResource ModernButtonStyle}"
                            Command="{Binding SalvarAsyncCommand}"
                            IsEnabled="{Binding IsSaving, Converter={StaticResource InverseBooleanConverter}}"
                            VerticalAlignment="Center"
                            MinWidth="120">
                        <StackPanel Orientation="Horizontal">
                            <icon:PackIconMaterial Kind="ContentSave"
                                                   Width="16"
                                                   Height="16"
                                                   Margin="0,0,8,0"
                                                   Visibility="{Binding IsSaving, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                            <icon:PackIconMaterial Style="{DynamicResource LoadingIconStyle}"
                                                   Width="16"
                                                   Height="16"
                                                   Margin="0,0,8,0"
                                                   Visibility="{Binding IsSaving, Converter={StaticResource BoolToVisibilityConverter}}" />
                            <TextBlock>
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text"
                                                Value="Salvar" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSaving}"
                                                         Value="True">
                                                <Setter Property="Text"
                                                        Value="Salvando..." />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Conteúdo principal -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Loading Indicator e Estado Vazio -->
            <views:LoadingAndEmptyStatePanel Grid.Row="0" />

            <!-- Campos do edital -->
            <Border Grid.Row="0"
                    Style="{DynamicResource PrimaryCardStyle}" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Sistema de Navegação com Botões Horizontais -->
                    <Grid Grid.Row="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <!-- Botões de Navegação -->
                        <Border Grid.Row="0"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="0,0,0,1"
                                Padding="0,0,0,16">
                            
                            <StackPanel Orientation="Horizontal">
                                <Button Name="BtnInformacoes"
                                        Command="{Binding NavigateToInformacoesCommand}">
                                    <StackPanel Orientation="Horizontal">
                                        <icon:PackIconMaterial Kind="Information"
                                                               Width="16"
                                                               Height="16"
                                                               VerticalAlignment="Center"
                                                               Margin="0,0,8,0" />
                                        <TextBlock Text="Informações" />
                                    </StackPanel>
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource NavigationTabButtonStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsInformacoesView}" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                
                                <Button Name="BtnAssuntos"
                                        Command="{Binding NavigateToAssuntosCommand}">
                                    <StackPanel Orientation="Horizontal">
                                        <icon:PackIconMaterial Kind="BookOpen"
                                                               Width="16"
                                                               Height="16"
                                                               VerticalAlignment="Center"
                                                               Margin="0,0,8,0" />
                                        <TextBlock Text="Assuntos" />
                                    </StackPanel>
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource NavigationTabButtonStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsAssuntosView}" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                
                                <Button Name="BtnCronograma"
                                        Command="{Binding NavigateToCronogramaCommand}">
                                    <StackPanel Orientation="Horizontal">
                                        <icon:PackIconMaterial Kind="CalendarClock"
                                                               Width="16"
                                                               Height="16"
                                                               VerticalAlignment="Center"
                                                               Margin="0,0,8,0" />
                                        <TextBlock Text="Cronograma" />
                                    </StackPanel>
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource NavigationTabButtonStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsCronogramaView}" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </Border>

                        <!-- Conteúdo das Seções -->
                        <Grid Grid.Row="1"
                              Margin="0,10,0,0"
                              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                              xmlns:views="clr-namespace:StudyMinder.Views">

                            <views:ViewEditalEditarInformacoes DataContext="{Binding}"
                                                               Visibility="{Binding IsInformacoesView, Converter={StaticResource BoolToVisibilityConverter}}"
                                                               d:Visibility="Hidden" />

                            <views:ViewEditalEditarAssuntos Visibility="{Binding IsAssuntosView, Converter={StaticResource BoolToVisibilityConverter}}"
                                                            d:Visibility="Visible" />

                            <views:ViewEditalEditarCronograma Visibility="{Binding IsCronogramaView, Converter={StaticResource BoolToVisibilityConverter}}"
                                                              d:Visibility="Hidden" />
                        </Grid>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
