<Window x:Class="StudyMinder.Views.MoverEventoDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:models="clr-namespace:StudyMinder.Models"
        xmlns:shell="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Height="480"
        Width="395"
        Background="{DynamicResource BackgroundBrush}">

    <!-- WindowChrome para barra de título customizada -->
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="32"
                            ResizeBorderThickness="5"
                            GlassFrameThickness="5"
                            CornerRadius="0"
                            UseAeroCaptionButtons="True" />
    </shell:WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Barra de título -->
        <Border Grid.Row="0"
                Background="{DynamicResource CardBrush}"
                Padding="8,4"
                BorderThickness="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Título -->
                <TextBlock Grid.Column="1"
                           Text="Reprogramar Evento"
                           Style="{DynamicResource Title3Style}"
                           Margin="0"
                           TextTrimming="CharacterEllipsis" />
            </Grid>
        </Border>

        <!-- Conteúdo principal -->
        <StackPanel Grid.Row="1">
            <Border Style="{DynamicResource PrimaryCardStyle}">
                <Calendar x:Name="DtNovaData"
                          Margin="0"
                          VerticalContentAlignment="Center" />
            </Border>

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="20">
                <Button Style="{StaticResource SecondaryButtonStyle}"
                        Content="Cancelar"
                        Click="BtnCancelar_Click"
                        Margin="0,0,12,0"
                        MinWidth="100" />

                <Button Style="{StaticResource ModernButtonStyle}"
                        Click="BtnConfirmar_Click"
                        IsEnabled="{Binding PodeMover}"
                        MinWidth="150">
                    <StackPanel Orientation="Horizontal">
                        <icon:PackIconMaterial Kind="ArrowRightBold"
                                               Width="16"
                                               Height="16"
                                               Margin="0,0,8,0"
                                               VerticalAlignment="Center" />
                        <TextBlock Text="Mover" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
