<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks">

    
    <!-- Container principal para Header de views -->
    <Style x:Key="ViewHeaderStyle" TargetType="Border">
        <Setter Property="Height" Value="55"/>
        <Setter Property="Margin" Value="20,0,20,0"/>
        <Setter Property="Padding" Value="0,5,0,10"/>
        <Setter Property="Background" Value="{DynamicResource HeaderBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
    </Style>
    
    <!-- Container principal para Footer de views -->
    <Style x:Key="ViewFooterStyle"
           TargetType="Border">
        <Setter Property="BorderBrush"
                Value="{DynamicResource BorderBrush}" />
        <Setter Property="BorderThickness"
                Value="0,1,0,0" />
        <Setter Property="Padding"
                Value="0,16,0,0" />
    </Style>
    
</ResourceDictionary>
